import React from 'react';
import useTasks from '../hooks/useTasks';

const Completed = () => {
    return (
        <div>
            <h2>This is Completed page.</h2>
        </div>
    );
};

export default Completed;